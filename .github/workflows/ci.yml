name: Java CI/CD with Maven and Kubernetes

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: write
  security-events: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Build with Maven
        run: mvn -B package --file pom.xml

      - name: Build Docker image
        uses: mr-smithers-excellent/docker-build-push@v6
        with:
          image: 1dlvb/async-bank-app
          registry: docker.io
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          tags: ${{ github.sha }}

  deploy:
    name: Deploy to Kubernetes
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up kubeconfig
        run: |
          mkdir -p ${HOME}/.kube
          echo ${{ secrets.KUBE_CONFIG }} | base64 --decode > ${HOME}/.kube/config
      
          USERNAME=$(kubectl config view --minify -o jsonpath='{.contexts[0].context.user}')
          TOKEN=$(kubectl config view --minify -o jsonpath='{.users[0].user.token}')
      
          kubectl config set-credentials ${USERNAME} --token=${TOKEN}
          kubectl config set-context my-context --user=${USERNAME} --cluster=k8s-cluster-6bd6-23f5f0ed9fe7
          kubectl config use-context my-context

      - name: Use context
        run: kubectl config use-context kubernetes-admin@kubernetes

      - name: Deploy to K8s
        run: kubectl apply -f k8s/ --validate=false
